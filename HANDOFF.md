# HANDOFF.md

Live status board for handing work between AI agents (Claude Code, ChatGPT Codex, Antigravity IDE, or any other tool used on this repo). See `AGENTS.md` for project background/rules — this file is just "where things stand right now."

**Rule: whichever agent picks up a session reads this file first, and whichever agent stops (task done, or approaching its context/usage limit) updates it before stopping.**

---

## Status: idle

_No agent currently mid-task. Last updated by: Claude (Claude Code) — 2026-07-25._

## Last completed

- **Added Paramount Movie Channel + MovieSphere by Lionsgate (2026-07-25).** Owner asked for "new movie channels", then specifically for Warner TV — which was declined (pay-TV via TrueVisions/AIS Play, no legit free stream; the only free "Warner" channels on Pluto are reality shows, not movies). Owner picked these two from a verified list of 12 Pluto US movie channels instead. Both use the existing `steam-hotel-pluto-proxy` (ids `5cb0cae7a461406ffe3f5213`, `645e7828e1979c00087b75b4`) — no new infrastructure. Verified pre-push: full chain master→media→segment with `ACAO: *`, ffprobe h264+aac through the proxy, logos 200. Deployed and confirmed live on Pages. **Not yet confirmed by the owner on the real hotel setup.** Note: the other 9 tested-working Pluto movie channel ids are in this session's transcript if the owner wants more later (Comedy, Drama, Thrillers, Crime, Romance, Fantastic, Horror, Classic Movies, Flicks of Fury, 80s Rewind all returned 200 via the proxy on 2026-07-25).
- **Added Amarin TV HD (channel 34) (2026-07-25).** `amarintv.com/live` embeds a fresh signed byteark URL (`x_ark_*` query, ~24h validity) directly in the page's Next.js payload on every load — same pattern as CH3's `ch3plus.com/live`. Built `workers/ch3-proxy`-style redirect worker at `workers/amarin-proxy/` (deployed as `steam-hotel-amarin-proxy.tiny-hall-8718.workers.dev`, KV namespace `AMARIN_TOKEN_CACHE`): scrapes `amarintv.com/live` for `playlist\.m3u8\?([^"]+)"`, caches the query in KV, 302-redirects to `amarin-ks7jcc.cdn.byteark.com` with a fresh token. `iptv.m3u8` points at `/live/720p_index.m3u8` (single-quality, same reasoning as 3HD — avoids ABR-related player quirks seen before). Verified end-to-end: worker → 302 → byteark URL → 200 with live manifest (increasing media-sequence numbers).
  - **Amarin had no audio at first (fixed 2026-07-25).** Amarin's audio is **demuxed**: `720p_index.m3u8` is video-only (confirmed with `ffprobe` — a single h264 stream, no audio), and the soundtrack lives in separate renditions (`Thai_HD_index.m3u8`, `Original_HD_index.m3u8`, `Narrative_HD_index.m3u8`) that a player only finds via the master playlist's `#EXT-X-MEDIA` AUDIO group. Pointing the channel straight at the video rendition — the same "pin to a single-quality endpoint" trick used for CH3 — therefore played silently. **This is the trap to remember: that trick is only safe when the variant is muxed.**
    - Fix: the worker now serves a **synthesised single-quality master** at `/live/playlist_720p.m3u8` — one 720p video variant plus the Thai audio rendition, both as absolute byteark URLs carrying the fresh token. Handing over Amarin's *real* master was rejected because it lists 1080p/6Mbps first and `startLevel: 0` in `index.html` would pin every viewer to it (high bitrate vs. the app's 10s buffer is exactly what caused the CH3 stutter, and multi-variant masters upset the owner's "M3U IPTV" Windows app).
    - The worker still never fetches byteark itself — it only templates the token in — so byteark's block on Cloudflare egress IPs (the 451 that broke CH3) can't affect this path, and segment traffic still leaves from the player's own Thai IP.
    - Verified: `ffprobe` on the worker URL reports 1 aac audio + 1 h264 1280x720 video, and decoding 6s of the audio gives 579,584 samples at mean −25.6 dB / max −13.5 dB (real sound, not a silent track). **Confirmed working by the project owner on the real hotel setup (2026-07-25).**
  - Note while debugging: byteark responds `403` to `HEAD` requests but `200` to `GET` on the same signed URL — don't mistake a `curl -I` 403 for a broken token, always verify with a real `GET`.
  - If this breaks: same playbook as CH3 — check whether `amarintv.com/live`'s HTML still contains a `playlist.m3u8?x_ark_...` query string; if Amarin changes their page structure, update the regex in `workers/amarin-proxy/worker.js`. Also watch for the same Cloudflare-egress geo-block byteark pulled on CH3 (451 from Workers' IPs) — this worker already redirects instead of proxying specifically to avoid that, but if Amarin's bucket is configured differently it could still surface.
- **Looked into ONE31 (ช่องวัน, channel 31) (2026-07-25): not added, no working stream URL found — someone can pick this back up, don't restart from scratch.** `one31.net/live` is a Nuxt SPA; the live player loads via `blob:` URLs (MSE) with no `.m3u8`/`.mpd` request visible in the network tab, so the manifest fetch itself is opaque to network inspection. Found a byteark player instance hardcoded in one JS chunk (`one31-rlbwkq.cdn.byteark.com/live/playlist.m3u8`) but every path variant tried against that CDN 404s directly — the component that references it (`playerOptions.sources.src` in chunk `3f51f07.js`) may be dead/legacy code for a different page, since the same page also wires up `DailyMotionLive`/`BrightcovePlayer` components, suggesting the live homepage may currently be running on a different backend (possibly Brightcove) than the byteark instance found. Whoever picks this up next: start by re-checking with browser devtools (Network tab, "Media" filter, not just XHR/Fetch) while the live homepage is open, since MSE segment fetches sometimes don't show up the same way as plain XHR.
- **Added PPTV HD 36 (2026-07-25).** Owner asked for it, pointing at `pptvhd36.com/live`. The stream URL isn't in that page's HTML — the live player is an iframe at `www-live.pptvhd36.com/api/live_player/program/1`, and the signed byteark URL lives in the player config inside it. New `workers/pptv-proxy/` scrapes it, caches the token in KV, and serves a synthesised single-quality master. See `AGENTS.md` § PPTV HD 36 auto-refresh manifest.
  - **Hit the demuxed-audio trap again** (third channel now: Amarin, then this). `720p/index.m3u8` is video-only per ffprobe; audio is a separate `audio-hi/th/index.m3u8` rendition. Caught before shipping this time by checking the master for `#EXT-X-MEDIA:TYPE=AUDIO` — worth making that a standard check on any new byteark channel.
  - Verified: worker returns 200 with `ACAO: *` and a token distinct from the one scraped by hand (proving it really re-fetches from Cloudflare egress — pptvhd36.com is not blocked to Workers, unlike byteark); `ffprobe` through the worker shows aac audio + h264 1280x720; decoding 6s of audio gives 532,480 samples at mean −25.0 dB / max −7.1 dB, so it's real sound on the Thai track.
  - Note PPTV's byteark manifest already sends `access-control-allow-origin: *`, so no CORS shim is needed here — the worker exists purely for token refresh.
  - **Confirmed working by the project owner (2026-07-25).**
- **Fixed every Pluto TV channel being unplayable in the web app — CORS (2026-07-25).** Owner showed Nickelodeon stuck on "กำลังเชื่อมต่อใหม่" in the web app. Cause: Pluto's stitcher answers `access-control-allow-origin: http://pluto.tv` (a specific foreign origin, not `*`), so the browser rejects the master playlist and hls.js never starts. **This affected all 6 Pluto entries** — Nickelodeon, UEFA Champions League, CBS Sports HQ, BBC News, Pluto TV Action Movies, Pluto TV Spotlight. Action Movies/Spotlight had presumably been broken in the web app for a long time unnoticed, because the owner watches through the M3U IPTV app where CORS isn't enforced.
  - Fix: new `workers/pluto-proxy/` (deployed as `steam-hotel-pluto-proxy.tiny-hall-8718.workers.dev`), and all six entries now point at `/<plutoChannelId>.m3u8` on it. See `AGENTS.md` § Pluto TV CORS shim for how it works and why the generic proxy can't.
  - Only manifests go through the worker; segments stay direct because `*.plutotv.net` already sends `access-control-allow-origin: *`. So this adds no meaningful Cloudflare bandwidth.
  - Verified: all 6 channel URLs return HTTP 200 with `ACAO: *`; the full chain master → media playlist → segment was walked by hand (segment fetched direct, 746KB, `ACAO: *`); `ffprobe` end-to-end through the proxy reports webvtt + aac + h264 854x480. Open-relay guard checked too — `example.com`, plain-http, and the suffix-spoof `pluto.tv.evil.com` are all refused with 400.
  - **Confirmed by the project owner (2026-07-25): every channel plays in the web app.**
  - Audit method worth reusing: replay the whole playlist with `curl -L -H "Origin: https://atsawinohio-dot.github.io"` and compare `access-control-allow-origin`. **Careful:** a response echoing the origin back verbatim is valid CORS, not a failure — only `*` or the exact origin passes. CH7 HD is the one remaining channel with no ACAO for our origin — see the CH7 entry under "Next up" before assuming that means it's dead; it plays fine outside the browser.
- **Auto-zoom past baked-in pillarbox bars (2026-07-25).** Owner asked whether Nickelodeon had "a new 16:9 link". It doesn't need one — **the feed is already 16:9 and no source swap can fix this.** `ffmpeg cropdetect` on the live channel shows a correct 1280x720 frame carrying only 960px of picture with **160px of black baked into each side** whenever old 4:3 material airs (all three Pluto variants are 16:9: 854x480, 854x480, 1280x720). The side pixels genuinely are black in the stream, so no `aspect-ratio`/`object-fit` change can recover them — note the earlier `object-fit: cover` switch did **nothing** for this channel for exactly that reason.
  - `index.html` now samples the frame into a 64x36 canvas every 2s and, when it sees symmetric black side bars, applies a CSS `scale()` to push them off screen. Nickelodeon mixes old 4:3 and modern 16:9 programming, so this must be per-frame — a per-channel setting would wrongly crop the modern shows.
  - Guards worth keeping if you touch it: a fade-to-black frame returns `null` (keep previous scale, don't jump); lopsided dark edges are ignored (real pillarboxing is symmetric); bars under 4% are treated as encoder noise; zoom is clamped to 1.45x; two consistent readings are required before committing a change. On iOS Safari's native-HLS path the canvas is cross-origin-tainted and `getImageData` throws — that's caught once and detection then disables itself rather than throwing every tick.
  - The maths lives in a pure `computePillarboxScale(data, w, h)` so it can be exercised directly. Verified two ways: 6 unit cases (including the exact measured 12.5%/side geometry → 1.3333x) and an in-browser test driving a synthetic `captureStream()` source, which zoomed to `scale(1.333)` on pillarboxed content and correctly released to no transform when the source switched to full-bleed 16:9.
- **BBC News now runs off BBC's own CDN (2026-07-25) — this supersedes the Pluto swap below.** Owner said they didn't want BBC News coming from Pluto TV. Replaced with the feed BBC's own apps use: `https://vs-hls-push-ww-live.akamaized.net/x=4/i=urn:bbc:pips:service:bbc_news_channel_hd/mobile_wifi_main_hd_abr_v2.m3u8` (BBC's Akamai, the `ww` = worldwide feed; the `-uk-live` siblings are UK-only, don't use those).
  - Strictly better than both previous sources, so there's no trade-off left to explain: **720p** top variant (vs Pluto's 1216x684 ceiling and antik's unusable 1080p), a **full ABR ladder from 192x108 to 1280x720** so a slow moment degrades instead of stalling, and it sends `Access-Control-Allow-Origin: *` so it needs **no proxy** and works in the web app and the M3U IPTV app alike.
  - Measured from the hotel network: media-playlist TTFB 0.13s, and a 2.53MB segment in 0.069s (~36 MB/s) — against ~0.4 MB/s on the old antik.sk source that started this whole thread. `ffprobe` confirms h264 + aac.
  - Found by filtering `https://iptv-org.github.io/api/streams.json` for BBC entries and keeping only BBC-owned hosts (`bbci.co.uk`, `vs-hls-*`/`vs-cmaf-*` Akamai). Everything else in that list is a bare-IP `http://` restream — still off-limits.
  - **Confirmed working by the project owner (2026-07-25).**
- **(superseded) Fixed BBC News loading slowly — swapped source (2026-07-25).** Owner reported "BBC News โหลดช้า". The old source (`dash2.antik.sk/live/test_bbc_world/playlist.m3u8`) is a **single-variant 1080p ~3.3Mbps** stream on a Slovak host, and it simply can't be pulled in real time from the hotel's network: measured 4 consecutive segments covering **20s of airtime taking 36.7s of wall time to download** (realtime ratio 1.83 — anything ≥1.0 means permanent rebuffering), wildly erratic (2.7s to 20.6s per segment), manifest TTFB ~650ms. It has no lower-bitrate variant to fall back to (`master.m3u8`, `index.m3u8`, `playlist_720p.m3u8` etc. all 404), so hls.js ABR has nothing to adapt down to.
  - Replaced with Pluto TV's official BBC News channel: `https://jmp2.uk/plu-65d92a8c8b24c80008e285c0.m3u8`. Same Pluto/FAST class as the other Pluto entries already in the list. Measured segment throughput ~45 Mbps (498KB in 0.087s) vs ~0.4 Mbps before, TTFB 0.07s. `ffprobe` confirms aac audio + h264 video + webvtt subs. **Owner confirmed every channel plays correctly after this change (2026-07-25).**
  - **Trade-off to be aware of: this is a resolution downgrade — 1920x1080 → 1216x684.** Pluto tops out at 1216x684 on all three of its variants (1.5/2.1/3.1 Mbps), so the picture is softer than the old feed's nominal 1080p. Accepted because the old feed's 1080p was unwatchable in practice. If a fast *and* genuinely-HD legitimate BBC source turns up, revisit.
  - Do **not** "fix" this by taking the BBC entries from `iptv-org`'s `uk.m3u` — they're bare-IP `http://` restreams (unauthorized redistribution per `AGENTS.md` § Content policy, and `http://` is blocked as mixed content on this HTTPS site anyway).
- **Checked TBS and Fuji TV (2026-07-25): declined, same as NHK Japanese.** Both only offer catch-up (TBS FREE / FOD), geo-locked to Japan, no live linear stream for overseas viewers. No further investigation needed unless a broadcaster announces an official international feed.
- **Looked into adding a Japanese-language NHK channel (2026-07-25): no legitimate free source exists — nothing added.** Findings, so nobody re-researches this:
  - The NHK entry already in `iptv.m3u8` ("NHK World-Japan HD") is the free international channel and it is **English-only**. Verified against NHK's own master playlist `https://master.nhkworld.jp/nhkworld-tv/playlist/live.m3u8` — every variant is `nhkwlive-ojp-en`, the only `#EXT-X-MEDIA` line is `LANGUAGE="en"` closed captions, and there is no alternate audio group. Probing `nhkwlive-ojp-{jp,ja,jpn}` returns 403 (no such rendition).
  - Japanese-language NHK TV is behind paywalls/geo-locks: **NHK Plus / NHK総合** requires a Japanese receiving-fee account and is Japan-only; **NHK World Premium** is a paid subscription (hotels get it via an official NHK Cosmomedia distribution contract, not a public stream URL). Both are out under `AGENTS.md` § Content policy.
  - NHK's Japanese radio (らじる★らじる, `radio-stream.nhk.jp`) doesn't resolve from outside Japan; the old `nhkworld.webcdn.stream.ne.jp/www11/radiojapan/...` HLS endpoints all 404 now.
- **Looked into adding Workpoint TV (channel 23, Thai) (2026-07-25): declined — requires bypassing anti-hotlink auth, same policy bucket as Mono29.** Don't re-attempt without a different source. Owner asked again later the same day (2026-07-25); re-verified: live page still embeds `play.sooplive.com/workpoint/embed`, and the only iptv-org entries are `v2h-cdn.com` / `thaimomo.com` — both known unauthorized restream aggregators, off-limits per content policy. Explained to owner (including why a YouTube-live link can't go in an M3U playlist) — **owner accepted, said no need to add it.** Closed unless Workpoint exposes a direct public HLS URL someday. Findings:
  - Workpoint's own live page (`workpointtv.com/live-stream-page`) just embeds a third-party platform: `https://play.sooplive.com/workpoint/embed` (SOOP, formerly AfreecaTV, a Korean live-streaming platform — not Workpoint's own CDN).
  - Traced SOOP's API chain: `POST live.sooplive.com/afreeca/player_live_api.php?bjid=workpoint` (bid=workpoint&type=live&player_type=html5&stream_type=common&quality=original) returns channel JSON with an `FTK` token and `RMD` host → `GET livestream-manager.sooplive.com/broad_stream_assign.html?return_type=gs_cdn&broad_key=295795847-common-original-hls` returns `{"view_url":"https://chromecast.stream.sooplive.com/live-stmc-30/auth_playlist.m3u8"}`.
  - That `auth_playlist.m3u8` URL 400s on a bare request (tried with `Referer` header and with the `FTK` token appended as `?aid=`, both failed) — response has `access-control-allow-credentials: true`, implying it's gated by session cookies bound to the browser/IP that requested it, not just a signed query string. Getting a working URL would mean simulating a full authenticated browser session against SOOP to defeat their anti-hotlink protection.
  - Per `AGENTS.md` § Content policy this is treated the same as the declined Mono29/DRM case: we don't circumvent access-control systems to pull a stream, even if the underlying content (Workpoint's own broadcast) is legitimately free-to-air. If Workpoint ever exposes a direct public HLS URL (not through SOOP's authenticated embed), reconsider.
- **Fixed CH3 again — byteark started geo-blocking Cloudflare Workers' egress (2026-07-20).** Owner reported "ch3 ดูไม่ได้" (can't watch). Diagnosis: the signed token from `ch3plus.com/live` was fine (verified valid directly), but every request the `ch3-proxy` worker made to `ch3-33-web.cdn.byteark.com` came back `451 Unavailable For Legal Reasons` — confirmed via `wrangler tail` and `wrangler dev --remote` that the worker always executes from Cloudflare's Singapore colo (`CF-RAY: ...-SIN`), and Cloudflare has no compute PoP inside Thailand, so there's no way to make the Worker's own fetch originate from a Thai IP (tried `[placement] mode = "smart"` first — did not help, reverted). Root cause: byteark now geo-fences the actual video CDN to Thailand IPs (confirmed: a request from a Thailand-geolocated IP got 200 with the exact same token that got the Worker 451). `ch3plus.com/live` itself (the token source page) is *not* geo-blocked, only the byteark video CDN is.
  - **Fix:** rewrote `workers/ch3-proxy/worker.js` from a full proxy into a **302 redirect**. The worker still scrapes/refreshes the signed token from `ch3plus.com/live` into KV exactly as before (that part was never broken), but instead of fetching and streaming byteark's manifest/segments itself, it now redirects the player straight to the real signed byteark URL. The actual video traffic then leaves from the player's own IP (the hotel's Thailand network), which passes byteark's geo-check. Bonus: this is simpler — no more manifest rewriting/reordering/prefetch-caching logic needed, since the channel is already pinned to the single-quality `playlist_720p` endpoint (see below) and byteark's own manifest already embeds a valid signed query on every segment URI.
  - Verified end-to-end after deploy: worker returns `302` → `Location` points at a fresh byteark URL → that URL returns `200` with a live manifest (increasing media-sequence numbers) → fetched an actual `.ts` segment referenced in it and confirmed a real MPEG-TS sync byte (`0x47`), 726 KB.
  - If this breaks again: check the same way — `curl -D- <worker-url>` for the status code, and `curl` the byteark URL directly from a Thailand-geolocated IP to tell "token problem" (fails from Thai IP too) apart from "geo-block problem" (works from Thai IP, fails from the worker). If byteark ever blocks Thailand IPs too or changes how it detects Cloudflare, this redirect approach stops working and there's no fix possible purely within Cloudflare Workers (no Thai compute PoP) — would need to host the redirect/proxy on a server that actually has a Thailand-based IP (e.g. a Thai VPS, or AWS's Bangkok region) instead.
  - Note: `iptv.m3u8` actually points CH3 at `/live/playlist_720p/index.m3u8` (not `playlist_480p` as older entries below still say — that was superseded by commits `41556d4`/`3eac330`, HANDOFF wasn't updated at the time). The `MAX_BANDWIDTH`/480p-cap logic mentioned below no longer exists in the worker — it's moot now since the channel is pinned directly to a single-quality endpoint upstream instead.
- Fixed 3HD (channel 3) end to end — this took several rounds, all confirmed fixed by the project owner testing the "M3U IPTV" Windows app on the real hotel network:
  1. **Dead stream URL**: old URL (`live-us1.thaimomo.com/...`) was 404, and every public mirror tried (v2h-cdn, byteark direct, etc.) was dead or 403'd. Real cause: ch3plus.com's official stream uses a server-side-signed URL that expires ~12h with no public token API. Fix: new Cloudflare Worker `workers/ch3-proxy/` (live at `steam-hotel-ch3-proxy.tiny-hall-8718.workers.dev`) that re-derives the signed URL from `ch3plus.com/live`'s HTML periodically and proxies the manifest/segments. See `AGENTS.md` § CH3 auto-refresh proxy.
  2. **Blurry picture**: the app's hls.js uses `startLevel: 0, testBandwidth: false` (index.html) — always starts on whichever quality is listed first, no probe. Upstream's master playlist listed 144p first. Fix: worker reorders master playlist variants highest-bandwidth-first.
  3. **Audio stutter, round 1**: 720p (3.5Mbps) left too little margin against the app's 10s hls.js buffer. Fix: worker caps the master playlist at 480p, dropping 720p entirely (not just reordering) so ABR can't climb back to it either.
  4. **Audio stutter, round 2 (rhythmic, ~8s cadence, only in the separate "M3U IPTV" Windows app — not on ch3plus.com directly and not in our own hls.js player)**: root cause turned out to be that app's own ABR/quality-switching logic choking on the multi-variant master playlist itself, not the proxy or stream. (Along the way also fixed two real proxy bugs worth keeping regardless: a `response.clone()` on the hot segment-serving path was teeing the stream and adding backpressure; the direct/cache-miss path wasn't forwarding the client's `Range` header to origin, so a lost prefetch race would silently return 200 instead of 206.) **Actual fix**: point the channel entry straight at a single-quality media playlist (`/live/playlist_480p/index.m3u8`) instead of the master, removing ABR from the picture entirely for that app.
- Fixed dead ROYS HOTEL logo URL (old `closte.com` CDN subdomain stopped resolving) — now points at `www.theroyshotel.com/wp-content/uploads/2026/03/ROYS-Logo.webp`.
- Looked into adding Mono29: declined. Mono29's official free stream (now hosted at monomaxsports.tv after a domain consolidation, but still genuinely free content) is DRM-protected (Widevine/FairPlay via ByteArk DRM), which this app's plain hls.js setup can't play and which we shouldn't try to circumvent. See AGENTS.md § Content policy.
- Added `AGENTS.md` + this `HANDOFF.md` so Claude Code / Codex / Antigravity can coordinate.
- Fixed channel-list-overlay "bounce" bug on iPhone (open-then-immediate-close double-fire, guarded with a 250ms same-gesture debounce in `openOverlay()`/`closeOverlay()`).
- Fixed iOS fullscreen: no longer calls `video.webkitEnterFullscreen()` (which hid our channel-picker UI); always uses the CSS pseudo-fullscreen fallback instead.
- Declined a request to add "MonoMax Sport TV" — every available stream URL traced back to unauthorized piracy-aggregator redistribution of a paid subscription service. Do not revisit this without a legitimate source (see AGENTS.md § Content policy).

## In progress

_Nothing in progress._

## Next up / open threads

- **Know which player the owner is looking at before diagnosing anything visual.** Confirmed 2026-07-25: the owner watches through the **"M3U IPTV" Windows app**, not this repo's web app. So `iptv.m3u8` and worker changes reach them, but **nothing in `index.html` does** — the 16:9 framing, `object-fit: cover`, the pillarbox auto-zoom and the `preconnect`/`preload`/ABR loading speed-ups all only affect guests using the web app/PWA. A screenshot with none of our UI in it (no "เปลี่ยนช่อง" button, no now-playing chip) is the tell. Don't report a client-side fix as solving something the owner sees in that app.
- Nothing queued. Ask the project owner what's next. As of 2026-07-25 the owner has confirmed all channels play correctly **in both the web app and the M3U IPTV app**.
- The pillarbox auto-zoom shipped but has only been verified synthetically (unit tests + a `captureStream()` source), never watched against a real 4:3 broadcast. If it ever looks over- or under-zoomed, `BAR_MAX_SCALE` / `BAR_LUMA` in `index.html` are the knobs.
- **CH7 HD is NOT dead — earlier entries in this file overstated it (re-checked 2026-07-25).** The existing entry points at CH7's own CDN (`live-cdn.ch7.com`), it is live, and it decodes end to end: `ffprobe` gives 1920x1080 h264 + aac, and the AES-128 key at `livekey.ch7.com` fetches fine (16 bytes). **It plays normally in the owner's M3U IPTV app; only the web app is affected.**
  - Cause is an origin allowlist, not a missing header: the CDN answers `access-control-allow-origin: https://www.ch7.com` when asked with CH7's own Origin, and sends nothing for ours. Deliberate, so there's nothing to "fix" upstream.
  - Can't be proxied through Cloudflare — re-tested 2026-07-25, CH7 still returns **403** to Worker egress. A fix needs a proxy on a genuinely Thai IP (Thai VPS / AWS Bangkok), i.e. new paid infrastructure. Not pursued.
  - No legitimate alternative source exists: the only other CH7 entries in iptv-org (`lb1-live-mv.v2h-cdn.com`, `live-us1.thaimomo.com`) are unauthorized restreams, and thaimomo is the same aggregator whose CH3 URL had already gone dead.
  - There *is* an unused master at `live-cdn.ch7.com/out/v1/<id>/playlist.m3u8` with a full ladder — 1080p (`playlist_9`, ~9 Mbps avg, 6.07MB per 6s segment), 720p (`playlist_10`, ~4.5 Mbps, 3.10MB), 540p (`playlist_11`), 432p (`playlist_12`), 360p. The entry is pinned to the heaviest, 1080p. **Owner was asked and chose to keep 1080p (2026-07-25)** — don't quietly downgrade it; note it's the heaviest channel in the playlist if bandwidth ever becomes an issue with many rooms watching at once.
- BBC News is running at 1216x684 rather than 1080p as a deliberate trade for a source that can actually stream in real time — see "Fixed BBC News loading slowly" above before changing it back.
- ONE31 (ช่องวัน) was investigated and not added; partial findings are recorded above so the next agent doesn't start from scratch.
- If `workers/ch3-proxy` ever breaks: `ch3plus.com/live`'s HTML probably changed and no longer contains `streamUrlWebAVOD"` — check the page source and update the regex in `workers/ch3-proxy/worker.js`.
- 3HD is currently pinned to 480p with no quality selector (see above) — if a future agent is tempted to switch it back to the adaptive master playlist for sharper picture, know that this previously caused unfixable-from-our-side stutter in the "M3U IPTV" Windows app specifically. Test in that exact app before changing it back.
- Known unfixed limitations (not currently being worked on, just tracked): CH7 HD **can't be proxied** (upstream 403s Cloudflare egress) so it's web-app-only-broken, not dead — see the CH7 entry above; Pluto TV Trending Now can't play (upstream blocks Cloudflare IP ranges, error 1042); Paramount+ Picks fails through the CORS proxy (upstream URL too long, HTTP 414). See `AGENTS.md` § CORS proxy.

## Handoff template

Copy this when you stop mid-task:

```md
## Status: in progress

_Picked up by: <tool name> — <date>._

## Last completed
- <what you just finished and verified working>

## In progress
- <exact task>, <why you're stopping: done for now / hit usage limit / blocked>
- Files touched so far: <paths>
- Anything half-done or uncommitted: <state, and whether it's safe to continue vs needs review first>

## Next up
- <the very next concrete step, written for someone with zero memory of this session>
```
